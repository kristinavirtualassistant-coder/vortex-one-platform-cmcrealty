# PostgreSQL-only authentication

Vortex One uses PostgreSQL as the sole authoritative identity, tenant, session, and application persistence layer. Firebase Authentication and Firestore are not part of the target runtime.
